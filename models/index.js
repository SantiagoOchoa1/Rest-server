const Categoria = require("./categoria.db");
const Role = require("./role.db");
const Usuario = require("./user.db");
const Server = require("./server");
const Producto = require("./producto.db");

module.exports = {
  Categoria,
  Role,
  Server,
  Usuario,
  Producto,
};
